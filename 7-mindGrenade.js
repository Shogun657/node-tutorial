const num1 = 5;
const num2 = 10;
function addValues(){
    console.log(`The sum is ${num1+num2}`)
}
addValues();

// if you declare the function in a module that you are going to import,
// it will run directly in the the module that it is being imported in.